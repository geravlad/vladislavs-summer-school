function testNumbers() {
  let result;
  if (numbers > 0) {
    result = "number is positive";
  } else {
    result = "number is negative";
  }
  return result;
}

console.log(testNumbers(1));

let numbers = 0;

const RESULT = numbers < 0 ? "number is positive" : "number is positive";

console.log;
